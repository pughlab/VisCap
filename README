Run command for Windows users:

RTerm.exe --file=VisCap.R --args [path to directory containing interval list files]

Run command for Linux users:

Rscript VisCap.R VisCap.cfg  /path/to/lane_dir  /path/to/output_dir

Note: The lane_dir is where the Depth of covrage files from Gatk DepthOfCoverage sit in. The output directory is
where you want the output results to be saved in.
